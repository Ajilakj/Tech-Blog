<section class="container">
  <section class="flex-row justify-center">
    {{#each blogData}}
    {{> blog-details}}
    {{/each}}
  </section>
        <div>
          <button id="new-blog-button" type="submit">New Post</button>
      </div>
</section>
